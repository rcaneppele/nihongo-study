# Deck inicial (seed)

Coloque aqui os dados de partida para importar com um clique:

- Conjunto completo de kana (já disponível em `src/data/kana.ts`).
- Um deck de vocabulário N5 em CSV/JSON.

Formato CSV: `front,back,reading,category,tags` (tags separadas por `;`).
